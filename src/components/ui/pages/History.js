import React, { Component } from "react";
import {
  Text  
} from "native-base"; 
export default class History extends Component{
    render(){
      return(
        <Text> History </Text>
      );
    }
}